# Existing dependencies
requests
pyyaml
openai
pydantic

# NEW - Research Intelligence System Dependencies

# Layer 1: Deep Paper Analysis
google-genai>=0.1.0         # NEW Gemini SDK with structured output support
PyMuPDF>=1.24.0             # PDF text extraction (fitz)

# Optional: Alternative LLM providers
# anthropic>=0.40.0         # Claude API (can configure in src/config.py)

# Layer 2: Bibliometric Analysis
networkx>=3.2             # Citation graphs and network analysis
pycombo                   # Community detection for research fronts (Combo algorithm)

# Layer 3: Visualization & Email
matplotlib>=3.8.0         # Graph visualization
seaborn>=0.13.0          # Enhanced plotting
# Email delivery uses smtplib (stdlib) via Gmail — no extra package needed

# M1a: Postgres migration (SQLite → Postgres + pgvector)
psycopg[binary]>=3.2      # Postgres driver (v3, supports sync + async)
pgvector>=0.3             # Python bindings for pgvector types
alembic>=1.13             # Schema migrations
python-dotenv>=1.0        # Load .env into os.environ

# M1b: FastAPI shell + Google OAuth + read-only API
fastapi>=0.115            # Web framework
uvicorn[standard]>=0.32   # ASGI server
pydantic-settings>=2.5    # .env-driven settings
SQLAlchemy>=2.0           # query layer (sync engine for now)
authlib>=1.3              # Google OAuth client
httpx>=0.27               # async HTTP for OAuth + tests
python-jose[cryptography]>=3.3   # JWT signing for session cookie
redis>=5.0                # health-check ping (full client comes in M2 with arq)
itsdangerous>=2.2         # signed-cookie helpers used by Authlib's StarletteMiddleware
