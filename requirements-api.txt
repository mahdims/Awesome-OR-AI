# M1b API container deps. Lean subset of requirements.txt — no layer1/2/3
# pipeline weight (pycombo, networkx, matplotlib, PyMuPDF, openai, google-genai).

# Web framework
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.0
pydantic-settings>=2.5
SQLAlchemy>=2.0

# Auth
authlib>=1.3
httpx>=0.27
python-jose[cryptography]>=3.3
itsdangerous>=2.2

# Postgres + migrations + pgvector
psycopg[binary]>=3.2
pgvector>=0.3
alembic>=1.13
python-dotenv>=1.0

# Redis (health-check ping; full client comes in M2)
redis>=5.0

# YAML for subdomains seed
pyyaml>=6.0

# Used by existing code we'll import (db.database, etc.)
requests>=2.31
